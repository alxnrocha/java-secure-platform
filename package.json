{
  "name": "vaultledger-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "VaultLedger — Enterprise Double-Entry Financial Engine & Security Platform",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "test": "npm --prefix client run test"
  }
}
